# ns-3 Artifacts

`contrib/information-routing` is the ns-3 module used for the paper's simulation
evaluation. It contains the C++ routing model, WAN experiment binary, test suite,
and Python sweep/analysis utilities.

The directory is arranged so it can be copied or symlinked directly into an
ns-3 checkout as `contrib/information-routing`.
