# Containerlab Artifacts

`srlinux-clos2x2` contains the SR Linux 2x2 Clos testbed used for the
product-router validation in the paper.

The paper-level runners in `../tools/` reference this topology by relative path,
so the experiment can be launched from any clone location.
