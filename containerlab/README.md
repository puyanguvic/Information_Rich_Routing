# Containerlab Artifact

`srlinux-clos2x2` contains the Nokia SR Linux 2x2 Clos testbed used for the
product-router validation.

The paper-level runners in `../tools/` reference this topology by relative path,
so experiments can be launched from any clone location. Generated lab outputs
should be written under `results/` or another ignored directory.
